# @visual-storyboard/core

Core TypeScript building blocks for visual-storyboard:

- storyboard event types
- pluggable transport interfaces
- a reusable storyboard writer
- a default file transport for NDJSON + asset output
