{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node --experimental-strip-types src/generate-sample.ts",
    "dev": "vp run generate && vp dev --port 4174",
    "build": "vp run generate && tsc && vp build",
    "preview": "vp preview --port 4174",
    "test": "vp test",
    "test:e2e": "vp exec playwright test"
  },
  "dependencies": {
    "visual-storyboard": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "typescript": "~5.9.3",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
